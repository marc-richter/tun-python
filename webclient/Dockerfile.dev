FROM node:20-alpine
WORKDIR /app

# Nur package.json zuerst (Cache für npm install)
COPY package.json ./
RUN npm install --no-audit --no-fund

# Rest nachziehen
COPY . .

EXPOSE 5173
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
